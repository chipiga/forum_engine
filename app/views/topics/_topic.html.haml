%article
  %header
    %h3= link_to topic.title, topic
  %section
  %footer
    %aside
      %time{ :datetime => topic.created_at.strftime('%Y-%m-%dT%H:%M:%S'), :class => 'created' }
        = l topic.created_at, :format => :long
      = topic.user.username.capitalize