%article
  %header
    %h3
  %section= post.body
  %footer
    %aside
      %time{ :datetime => post.created_at.strftime('%Y-%m-%dT%H:%M:%S'), :class => 'created' }
        = l post.created_at, :format => :long
      = post.user.username.capitalize