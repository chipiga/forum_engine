%article
  %header
    %h3= link_to forum.name, forum
  %section= forum.description
  %footer
    %aside
      %time{ :datetime => forum.created_at.strftime('%Y-%m-%dT%H:%M:%S'), :class => 'created' }
        = l forum.created_at, :format => :long
      / = forum.user.username.capitalize